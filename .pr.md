## Assignment Summary
- Removed a stored XSS exposure in profile bios by keeping user-controlled content as plain text and stopping the AJAX endpoint from echoing raw bio data back to the browser.

## Related Issue
- Assignment issue not provided in the prompt.

## Target Assignment Branch
- assignment/fix-stored-xss-profile-content

## Design Note
- Kept the profile bio on Django's normal escaping path instead of introducing any unsafe HTML rendering shortcut.
- Stopped the profile AJAX update response from reflecting the saved bio, since the front end only needs a success message.
- Added a regression test that stores a malicious bio string and verifies it is preserved as text but rendered escaped in the HTML response.

## Security Impact
- **Stored Content Stays Non-Executable**: A saved bio like `<script>...` is persisted as text and rendered escaped, not as markup.
- **Less Reflected User Input**: The AJAX endpoint no longer echoes raw profile text back to the browser.
- **Normal Text Still Works**: Legitimate profile text is still stored unchanged and displayed safely.

## Changes Made
- Updated `charles/views.py` so the AJAX profile update response only returns status metadata.
- Added a regression test in `charles/tests.py` that saves malicious profile text and confirms the response escapes it.
- Updated the AJAX profile test to match the safer response payload.

## Validation
- **Unit Testing**: 62 tests pass total.
- Verified that a malicious bio is stored verbatim but rendered as escaped text in the profile page response.
- Verified that the AJAX profile update endpoint still succeeds after the response payload change.
- Verified that the full Django test suite still passes after the change.

## AI Assistance Used
- Yes, within a limited and defined scope.

## What AI Helped With
- Helped identify the safest place to tighten the stored XSS surface without changing normal profile editing behavior.
- Assisted with drafting the regression test cases and the PR wording.

## What I Changed From AI Output
- Kept the fix minimal by removing the unnecessary raw bio echo instead of adding a new rendering layer.
- Chose a regression test that proves the bio stays plain text in storage and escaped in HTML.
- Left the normal form flow intact so existing profile behavior still works.

## Security Decisions I Made Myself
- Chose output escaping over input stripping so normal text handling stays intact.
- Removed a reflected copy of the user-controlled bio because the front end did not need it.
- Verified the profile response still shows the stored value as text rather than executable markup.

## Authorship Affirmation
- I understand where the profile bio is stored, how it is rendered safely, and which tests prove that stored HTML does not execute.

## Checklist
- [x] I started from the active assignment branch for this task
- [x] My pull request targets the exact assignment branch named in the prompt
- [x] I included a short design note and meaningful validation details
- [x] I disclosed any AI assistance used for this submission
- [x] I can explain the key code paths, security decisions, and tests in this PR
- [x] I tested the change locally
- [x] I updated any directly related documentation or configuration, or none was required
